## インストール（Installation）

Swiper をプロジェクトに組み込む方法はいくつかあります。

---

## NPM からインストールする（Install from NPM）

Swiper は NPM からインストールできます。

```bash
npm install swiper
```

```js
// Swiper の JS を読み込む
import Swiper from 'swiper';

// Swiper の CSS を読み込む
import 'swiper/css';

const swiper = new Swiper(...);
```

### モジュールについて（重要）

デフォルトでは、Swiper は **コア機能のみ** をエクスポートします。  
（Navigation、Pagination などの追加機能は含まれません）

そのため、必要なモジュールは **個別に import して設定する必要があります。**

```js
import Swiper from 'swiper';
import { Navigation, Pagination } from 'swiper/modules';

// CSS
import 'swiper/css';
import 'swiper/css/navigation';
import 'swiper/css/pagination';

// Swiper 初期化
const swiper = new Swiper('.swiper', {
  modules: [Navigation, Pagination],
});
```

---

### すべてのモジュールを使いたい場合（bundle）

すべてのモジュールを含んだ状態で使う場合は、`swiper/bundle` を使います。

```js
import Swiper from 'swiper/bundle';
import 'swiper/css/bundle';

const swiper = new Swiper(...);
```

---

## CDN を使う（Use Swiper from CDN）

Swiper のファイルをプロジェクトに含めたくない場合、CDN を使えます。

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@12/swiper-bundle.min.css" />
<script src="https://cdn.jsdelivr.net/npm/swiper@12/swiper-bundle.min.js"></script>
```

### ES Modules を使う場合

```html
<script type="module">
  import Swiper from 'https://cdn.jsdelivr.net/npm/swiper@12/swiper-bundle.min.mjs'
  const swiper = new Swiper(...)
</script>
```

---

## アセットをダウンロードする（Download assets）

Swiper のファイルをローカルで使いたい場合は、以下から直接ダウンロードできます。

👉 [https://www.jsdelivr.com/package/npm/swiper](https://www.jsdelivr.com/package/npm/swiper)

---

## Swiper の HTML 構造を追加する（Add Swiper HTML Layout）

次に、**基本となる HTML 構造**を追加します。

```html
<!-- スライダー全体 -->
<div class="swiper">
  <!-- 必須のラッパー -->
  <div class="swiper-wrapper">

    <!-- 各スライド -->
    <div class="swiper-slide">Slide 1</div>
    <div class="swiper-slide">Slide 2</div>
    <div class="swiper-slide">Slide 3</div>

  </div>

  <!-- ページネーションが必要な場合 -->
  <div class="swiper-pagination"></div>

  <!-- ナビゲーションボタンが必要な場合 -->
  <div class="swiper-button-prev"></div>
  <div class="swiper-button-next"></div>

  <!-- スクロールバーが必要な場合 -->
  <div class="swiper-scrollbar"></div>
</div>
```

---

## Swiper の CSS / サイズ設定（Swiper CSS Styles / Size）

Swiper の CSS に加えて、**サイズ指定の CSS が必要**です。

```css
.swiper {
  width: 600px;
  height: 300px;
}
```

⚠️ **高さを指定しないと表示されません（重要）**

---

## Swiper を初期化する（Initialize Swiper）

最後に、JavaScript で Swiper を初期化します。

```js
const swiper = new Swiper('.swiper', {
  // オプション設定
  direction: 'vertical',
  loop: true,

  // ページネーション
  pagination: {
    el: '.swiper-pagination',
  },

  // ナビゲーションボタン
  navigation: {
    nextEl: '.swiper-button-next',
    prevEl: '.swiper-button-prev',
  },

  // スクロールバー
  scrollbar: {
    el: '.swiper-scrollbar',
  },
});
```
