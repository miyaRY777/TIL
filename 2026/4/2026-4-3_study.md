# 今日やったこと

* `bin/rails db:prepare` の役割を学習した
* データベース準備の流れ（作成・マイグレーション・seed）を理解した

# 今日理解できたこと

* `db:prepare` は「DBをまとめて整えるコマンド」である
* DBが存在しない場合は作成される
* 既に存在する場合はマイグレーションのみ実行される
* 開発環境やテスト環境の初期セットアップに便利

# まだ理解できていないこと

* `db:prepare` の内部でどのタスク（`db:create` や `db:migrate` など）がどの順番で呼ばれているのか
* `seed` が実行される条件（毎回なのか、特定条件のみなのか）

# 次にやること

* `db:create` / `db:migrate` / `db:seed` の違いを整理する
* Rails GuideでDB関連コマンドの流れを確認する
* 実際にDBを削除 → `db:prepare` を実行して挙動を確認する

# 感想など

* 「とりあえずこれを叩けばOK」という便利コマンドがあるのは安心感がある
* ただ中身を理解していないとトラブル時に困りそうなので、内部の流れもちゃんと理解したい
