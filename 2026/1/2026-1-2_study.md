#PostgreSQL #LEFT_JOIN

LEFT JOIN（LEFT OUTER JOIN）とは何か

### PostgreSQL公式の定義（要旨）

> **LEFT OUTER JOIN** returns all rows from the left table, plus matching rows from the right table.  
> If there is no match, the right side columns are filled with NULLs.

---

### 日本語で正確に言うと

> **左側テーブルの行は必ず残し、右側に一致がなければ NULL を入れる**

---

### SQL例

```ruby
SELECT * FROM posts 
LEFT JOIN comments   
ON comments.post_id = posts.id;
```
`SELECT * FROM posts LEFT JOIN comments   ON comments.post_id = posts.id;`

---
### データ例

### **posts**

|id|title|
|---|---|
|1|A|
|2|B|
|3|C|

### **comments**

|id|post_id|body|
|---|---|---|
|10|1|x|
|11|1|y|
|12|2|z|
### LEFT JOIN の結果

|post_id|title|comment_id|body|
|---|---|---|---|
|1|A|10|x|
|1|A|11|y|
|2|B|12|z|
|3|C|NULL|NULL|

👉 **post_id = 3 が残る（重要）**