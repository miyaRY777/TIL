# ステップ① PoC用Railsアプリを作成する(７系)

## 1️⃣ Rails 7.2 が使えるか確認（念のため）

```bash
rails -v
```

- `Rails 7.2.x` が表示されればOK
- もし複数バージョンが入っていても、次で指定するので問題ありません

---

## 2️⃣ Rails 7.2 を指定してアプリ作成

```bash
rails _7.2.0_ new tag_poc -d postgresql
```

### ここでやっていること

- `tag_poc`：PoC用アプリ名
- `-d postgresql`：DBをPostgreSQLに指定
- `rails _7.2.0_`：Rails 7.2を明示指定

👉 **この時点では `bin/rails` は使いません（まだ存在しないため）**

---

## 3️⃣ アプリディレクトリに移動

```bash
cd tag_poc
```

ここから先は **すべてこのアプリ専用**なので  
`bin/rails` を使っていきます。

---

## 4️⃣ PostgreSQL が起動しているか確認

```bash
psql --version
```

もし PostgreSQL が入っていない場合（Mac）：

```bash
brew install postgresql
brew services start postgresql
```

補足

⚪︎状態確認したい場合

起動中か確認
`brew services list`

表示例：
`Name         Status  User     File postgresql  started miya     ~/Library/LaunchAgents/homebrew.mxcl.postgresql.plist`

---
⚪一時的に止めたいだけなら（再起動せず）
`brew services stop postgresql`

👉 次回ログイン時も起動しません。

---
⚪もう一度起動したいとき
`brew services start postgresql`

---

## 5️⃣ データベースを作成

```bash
bin/rails db:create
```

### 成功すると

```text
Created database 'tag_poc_development'
Created database 'tag_poc_test'
```

👉 **PoCの土台が完成**

---

## 6️⃣ サーバーを起動して動作確認

```bash
bin/rails s
```

ブラウザでアクセス：

```
http://localhost:3000
```

Railsの初期画面が表示されれば成功 🎉
