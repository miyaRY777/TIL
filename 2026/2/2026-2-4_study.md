### 外部制約キーとは？

結論から言うと：
別のテーブルの主キー（idなど）を参照して、
存在しないデータを勝手に入れられないようにする制約

### Railsのmigration で書くなら？
```ruby
t.references :user, null: false, foreign_key: true
```
user_id カラム作成
index 作成
外部制約キー（foreign key）追加

