# アクセサメソッドとは何か？

## 結論から

**アクセサメソッド**とは、

> **インスタンス変数（@name など）を  
> 外から安全に「読む・書く」ための仕組み**

です。

Rubyでは、**インスタンス変数は直接触れません。**

```ruby
hero.@name   # ← これはできない
```

だから「間に入るメソッド」が必要になります。

---

# まずは普通の方法（アクセサなし）

```ruby
class Character
  def initialize(name)
    @name = name
  end

  def get_name
    @name
  end

  def set_name(name)
    @name = name
  end
end
```

### ここで起きていること

- `get_name` → **読むためのメソッド**
- `set_name` → **書くためのメソッド**

これらを **自分で毎回書くのは面倒** です

---

# attr_accessor の正体

```ruby
attr_accessor :name
```

これは実は👇と **同じ意味**です。

```ruby
def name
  @name
end

def name=(value)
  @name = value
end
```

つまり：

- `hero.name` → getter
- `hero.name = "勇者マサシ"` → setter

を **自動生成**してくれます。

---

# attr_accessor を使うと何が嬉しい？

### before（自力で書く）

```ruby
def get_name; @name; end
def set_name(name); @name = name; end
```

### after（1行）

```ruby
attr_accessor :name
```

👉 **短い・読みやすい・ミスしない**

---

# 読み取り専用・書き込み専用の意味

## attr_reader（読むだけ）

```ruby
attr_reader :hp
```

```ruby
hero.hp      # OK
hero.hp = 200  # ❌ エラー
```

### 使う場面

- 外から変更されたくない値
- ステータス・計算結果

---

## attr_writer（書くだけ）

```ruby
attr_writer :job
```

```ruby
hero.job = "魔法使い"  # OK
hero.job               # ❌ エラー
```

### 使う場面（少しレア）

- 外から設定だけさせたい
- 読ませたくない内部情報

---

## attr_accessor（読む・書く両方）

```ruby
attr_accessor :name
```

```ruby
hero.name        # OK
hero.name = "新名" # OK
```

### 一番よく使う

---

# なぜ「直接 @name を使わせない」のか

理由は **安全のため**です。

```ruby
hero.name = ""
```

ここに後から：

```ruby
def name=(value)
  raise "空はダメ" if value.empty?
  @name = value
end
```

とルールを足せます。

👉 **中身を守れる**

---

# Railsでの超重要な使い道

Railsではこれが頻出します👇

```ruby
attr_accessor :tag_list
```

### なぜ？

- DBに保存しない
- フォームで使いたいだけ
- 一時的な値

👉 **「仮の入れ物」**として使う

---

# 使い分けの目安（超重要）

|目的|使うもの|
|---|---|
|普通の値|attr_accessor|
|外から変更禁止|attr_reader|
|書くだけさせたい|attr_writer|
|フォーム専用|attr_accessor|
|内部だけ|何も書かない|
